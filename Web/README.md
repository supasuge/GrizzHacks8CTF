# Web Challenges

## TODO
- [ ] Add a `docker-compose.yml` file to easily deploy all web challenges at once + add additonal sandbox protections to prevent potential docker escape (**highly** unlikely but not hard to add so... meh)
- [ ] Add 1-2 much easier challenges with simple `Information Disclosure` vuln, and maybe a `File Upload` vulnerability.
- [ ] Better/more readable + understandable writeups with *slightly* less sarcasm (likely not, will probably add more kekW)
